function moveToRace(year, round) {
    window.location.href = "/f1app/race/" + year + "/" + round + "/";
}

function moveToDriver(driver) {
    window.location.href = "/f1app/driver/" + driver + "/";
}

function moveToConstructor(constructor) {
    window.location.href = "/f1app/constructor/" + constructor + "/";
}